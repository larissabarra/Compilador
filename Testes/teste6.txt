program
 a, b, c, maior : int;
 {
 scan(a);
 scan(b);
 scan(c;

 maior = 0;
 if ( a>b && a>c ){
 maior := a;
 }
 else{
 if (b>c){
 maior := b;
 }
 else{
 maior := c;
 }
 };
 print(“Maior idade: ”);
 print(maior);